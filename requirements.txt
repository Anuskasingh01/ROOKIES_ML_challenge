# Core data
pandas>=2.0.0
numpy>=1.24.0

# Candidate generation & blocking (Person 2)
# Uses stdlib only (collections, heapq, re, unicodedata) - no external services or network calls.

# String similarity (Person 3) — rapidfuzz chosen over difflib for 10-100x speed
# improvement on large candidate sets (same API surface as difflib's SequenceMatcher).
rapidfuzz>=3.0.0

# ML / feature engineering (Person 3)
scikit-learn>=1.3.0
